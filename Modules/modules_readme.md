# modules package readme

## Settup
cd "path to modules folder in double quotation signs"  
pip install . (installes setup.py files in current directory)

## import
import modules.messstatistik as ms
